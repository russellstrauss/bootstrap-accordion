(function () {
	// For accordions with long content, scroll to the top of the content after opening
	// $("#accordion").on("shown.bs.collapse", function () {
	// 	var el = $(this).find('.collapse.in').closest('.panel');
	// 	$('html, body').scrollTop($(el).offset().top);
	// });
	
	// Or the same feature as above, but with animation
	// $("#accordion").on("shown.bs.collapse", function () {
	// 	var myEl = $(this).find('.collapse.in').closest('.panel');

	// 	$('html, body').animate({
	// 		scrollTop: $(myEl).offset().top
	// 	}, 500);
	// });
})();